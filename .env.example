# Copy this file to .env and fill in your values
# These are used server-side only (never sent to the browser)

# Optional: Server-side Telnyx API key (used as fallback if user has none configured)
TELNYX_API_KEY=KEYxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Node environment
NODE_ENV=development
